# Supabase Migration Engine Rebuild - Complete Instructions

## ✅ Verification Complete

### 1. Local Migration Files Verified

All 13 migration files exist and match the remote list:

- ✅ `20241117_add_core_tables.sql`
- ✅ `20250101_drop_sensitive_tables.sql`
- ✅ `20250101000000_vella_core_admin.sql`
- ✅ `20250217_token_engine.sql`
- ✅ `20250218_add_adaptive_traits.sql`
- ✅ `20250219_add_nudge_history.sql`
- ✅ `20250220_add_feature_tables.sql`
- ✅ `20250221_add_progress_features.sql`
- ✅ `20250222_add_last_active_at.sql`
- ✅ `20250223_remove_checkin_note.sql`
- ✅ `20251129154622_create_admin_global_config.sql`
- ✅ `20251219_drop_legacy_vella_settings_fields.sql`
- ✅ `20251220_fix_vella_settings.sql`

### 2. Config File Verified

✅ `supabase/config.toml` is clean - no `[migration]` or `[migrations]` blocks exist.

---

## 📝 Scripts Created

### Script Files:

1. **`scripts/supabase/rebuild-new-engine.ts`** - TypeScript version (requires compilation)
2. **`scripts/supabase/rebuild-new-engine.js`** - JavaScript version (ready to run)
3. **`scripts/supabase/rebuild-new-engine.py`** - Python version (ready to run)
4. **`scripts/compute-migration-hashes.js`** - Simplified JavaScript version (recommended)

---

## 🚀 How to Generate the SQL File

### Option 1: Run the Node.js Script (Recommended)

```bash
node scripts/compute-migration-hashes.js
```

This will:
- Read all 13 migration files
- Compute SHA256 hashes for each
- Generate `supabase/rebuild_migration_engine.sql`
- Generate `supabase/validate_migrations.sql`

### Option 2: Run the Python Script

```bash
python scripts/supabase/rebuild-new-engine.py
```

### Option 3: Run the TypeScript Script (requires compilation)

```bash
# First compile TypeScript
npx tsc scripts/supabase/rebuild-new-engine.ts
# Then run
node scripts/supabase/rebuild-new-engine.js
```

---

## 📄 Generated Files

After running the script, you will have:

1. **`supabase/rebuild_migration_engine.sql`** - Complete rebuild script
2. **`supabase/validate_migrations.sql`** - Validation queries

---

## 🎯 SQL Script Contents

The generated `rebuild_migration_engine.sql` will:

1. **DROP** `supabase_migrations.schema_migrations` table (if exists)
2. **CREATE** `supabase_migrations` schema (if not exists)
3. **CREATE** `supabase_migrations.migrations` table with schema:
   - `id` bigint (auto-increment)
   - `version` text
   - `name` text
   - `hash` text (SHA256)
   - `executed_at` timestamptz
   - `statements` text (full SQL content)
4. **TRUNCATE** existing data
5. **INSERT** all 13 migrations with:
   - Computed SHA256 hashes
   - Full SQL statements (escaped)
   - Current timestamp

---

## ⚙️ How to Execute in Supabase

### Step 1: Generate the SQL File

Run one of the scripts above to generate `supabase/rebuild_migration_engine.sql`.

### Step 2: Open Supabase SQL Editor

1. Go to your Supabase project dashboard
2. Navigate to **SQL Editor**
3. Click **New Query**

### Step 3: Paste and Run

1. Open `supabase/rebuild_migration_engine.sql`
2. Copy the entire contents
3. Paste into the SQL Editor
4. Click **Run** (or press `Ctrl+Enter`)

### Step 4: Verify

Run the validation script:

1. Open `supabase/validate_migrations.sql`
2. Copy and paste into SQL Editor
3. Run to verify all 13 migrations are inserted correctly

---

## ✅ Expected Results

After execution:

- `supabase_migrations.migrations` table will contain exactly 13 rows
- Each row will have:
  - Unique `version` (timestamp)
  - `name` (migration name)
  - `hash` (SHA256 hash of the SQL file)
  - `executed_at` (current timestamp)
  - `statements` (full SQL content)

---

## 🔍 Validation Queries

After running the rebuild script, verify with:

```sql
-- Count migrations
SELECT COUNT(*) as total_migrations 
FROM supabase_migrations.migrations;

-- List all migrations
SELECT version, name, hash, executed_at 
FROM supabase_migrations.migrations 
ORDER BY version;

-- Check for duplicates
SELECT version, COUNT(*) as count
FROM supabase_migrations.migrations
GROUP BY version
HAVING COUNT(*) > 1;
```

Expected: 13 migrations, no duplicates.

---

## 📋 Summary

**Files Created:**
- ✅ `scripts/supabase/rebuild-new-engine.ts`
- ✅ `scripts/supabase/rebuild-new-engine.js`
- ✅ `scripts/supabase/rebuild-new-engine.py`
- ✅ `scripts/compute-migration-hashes.js`

**Files to Generate (run script first):**
- ⏳ `supabase/rebuild_migration_engine.sql` (generated by script)
- ⏳ `supabase/validate_migrations.sql` (generated by script)

**Config Verified:**
- ✅ `supabase/config.toml` - Clean, no migration blocks

**Next Steps:**
1. Run `node scripts/compute-migration-hashes.js`
2. Open `supabase/rebuild_migration_engine.sql` in Supabase SQL Editor
3. Execute the SQL script
4. Verify with `supabase/validate_migrations.sql`

---

## ⚠️ Important Notes

- **Do NOT modify** the SQL content of any migration file
- **Do NOT normalize** whitespace in migration files
- **Do NOT trim** or alter the statements arrays
- The script preserves exact file contents and computes accurate SHA256 hashes
- All migration filenames and timestamps match exactly with the remote list

